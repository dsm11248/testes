//Importar o React
import React from 'react';

//Importar as dependencias do 'react-router-dom'
import {BrowserRouter as Router, Route} from 'react-router-dom';

//Importar os componentes da aplicação

const Routes = () => {
    <BrowserRouter>
        <Route path='' component='' />
    </BrowserRouter>
}

export default Routes;